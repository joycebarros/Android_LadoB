<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android_ladob">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.FazerPedidoActivity"></activity>
        <activity android:name=".view.teste_addPedido"/>
        <activity android:name=".view.ProductOrderActivity" />
        <activity android:name=".view.AddProductsActivity" />
        <activity android:name=".view.PagamentoActivity" />
        <activity android:name=".view.ProductsActivity" />
        <activity android:name=".view.AddCostumersActivity" />
        <activity android:name=".view.CostumersActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>